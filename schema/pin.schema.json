{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "title": "Pin",
  "type": "object",
  "required": [
    "digest",
    "direction",
    "fails_closed",
    "generated_at",
    "generated_by",
    "id",
    "rows",
    "rows_considered",
    "rows_enforced",
    "source_of_record",
    "subject",
    "target_of_record"
  ],
  "properties": {
    "digest": {
      "type": "string"
    },
    "direction": {
      "$ref": "#/definitions/PinDirection"
    },
    "fails_closed": {
      "description": "Always true. A pin that does not fail closed is a third opinion.",
      "type": "boolean"
    },
    "generated_at": {
      "$ref": "#/definitions/Timestamp"
    },
    "generated_by": {
      "description": "The exact command that regenerates this pin byte for byte (VDS S-2(5)(4)).",
      "type": "string"
    },
    "id": {
      "$ref": "#/definitions/PinId"
    },
    "proof_id": {
      "default": null,
      "anyOf": [
        {
          "$ref": "#/definitions/ProofId"
        },
        {
          "type": "null"
        }
      ]
    },
    "rows": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/PinRow"
      }
    },
    "rows_considered": {
      "type": "integer",
      "format": "uint64",
      "minimum": 0.0
    },
    "rows_enforced": {
      "type": "integer",
      "format": "uint64",
      "minimum": 0.0
    },
    "source_of_record": {
      "$ref": "#/definitions/RecordOfTruth"
    },
    "subject": {
      "type": "string"
    },
    "target_of_record": {
      "$ref": "#/definitions/RecordOfTruth"
    }
  },
  "additionalProperties": false,
  "definitions": {
    "PinDirection": {
      "oneOf": [
        {
          "description": "The only lawful direction. A pin derives one way, from the named records, and never feeds a value back into either.",
          "type": "string",
          "enum": [
            "one_way_derived"
          ]
        }
      ]
    },
    "PinId": {
      "description": "A derived one-way agreement assertion between two named records.",
      "type": "string",
      "pattern": "^PIN-[0-9]{8}-[0-9]{6}$"
    },
    "PinRow": {
      "description": "One compared row.\n\nThe name of the thing compared, and whether the two records agreed about it. There is deliberately nowhere here to put a value or a digest of one; see the module documentation.",
      "type": "object",
      "required": [
        "agrees",
        "name"
      ],
      "properties": {
        "agrees": {
          "type": "boolean"
        },
        "name": {
          "description": "The NAME of the token, boundary or property compared. A name, never a value: `control-border`, not `#ebebeb`.",
          "type": "string"
        },
        "not_enforced_because": {
          "description": "Present only where the row was NOT enforced, saying why. A row silently excluded is a row nobody can audit, and a pin full of them looks identical to a pin that passed.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "ProofId": {
      "description": "A captured proof result, stamped with the UTC second it was captured.",
      "type": "string",
      "pattern": "^PROOF-[0-9]{8}-[0-9]{6}$"
    },
    "RecordOfTruth": {
      "description": "A named system of record. VDS S-2(3) fixes the set, and it is not VDS's to move: relocating a source of truth is a referral, not an implementation.",
      "type": "object",
      "required": [
        "authority_for",
        "digest",
        "locator"
      ],
      "properties": {
        "authority_for": {
          "description": "What this record is the system of record FOR, e.g. \"what ships\".",
          "type": "string"
        },
        "digest": {
          "description": "The digest of the record AS A WHOLE, as read when the pin was generated.\n\nSafe where a per-value digest is not: a file is not a low-entropy domain, so this cannot be inverted to recover any value inside it. It answers \"did this record move\", which is a different question from \"what does this record say\".",
          "type": "string"
        },
        "locator": {
          "description": "How to reach it: a repository-relative path, or a Figma file key.",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "Timestamp": {
      "description": "UTC, second resolution, trailing Z. The one canonical form VDS writes.",
      "type": "string",
      "format": "date-time",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$"
    }
  }
}
