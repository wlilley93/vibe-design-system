{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "title": "VisualReviewRecord",
  "description": "One recorded visual pass, against one frame.",
  "type": "object",
  "required": [
    "fileKey",
    "frameDigest",
    "frameImageDigest",
    "id",
    "nodeId",
    "reviewedAt",
    "reviewedBy",
    "routes",
    "shippedScreenshotDigest",
    "shippedSourceDigest",
    "verdict"
  ],
  "properties": {
    "basis": {
      "default": [],
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "contractSignoff": {
      "description": "THE STAGE-1 CONTRACT VERSION this verdict was taken against: the sign-off row whose hash is the signed frame the reviewer compared to.\n\nThe pipeline is four stages in fixed order - contract JSON, then Figma AND code built to it, then source-side gates, then this artefact-side visual check - and the stages must be LINKED, not parallel. A verdict that cannot name its contract version is how \"we checked it\" survives a contract change with nobody noticing: the frame is redrawn, the contract moves, and a verdict rendered against the old one keeps reading as coverage.\n\n`Option` because a `no_authority` verdict is precisely the state of having NO contract to cite, and forcing a citation there would teach a pipeline to invent one. Any other verdict without it is invalid.",
      "anyOf": [
        {
          "$ref": "#/definitions/SignoffId"
        },
        {
          "type": "null"
        }
      ]
    },
    "deltas": {
      "description": "What differed, one row per difference. Required non-empty for `deviate` and required EMPTY for `conform`: a verdict and its evidence may not disagree.\n\nRows and not strings. A bare list answered \"what differs\" and left every difference reading as work owed by the code, which is false of most of them; see [`DeltaDisposition`] for what the classification buys and for why it can never dispose of anything.",
      "default": [],
      "type": "array",
      "items": {
        "$ref": "#/definitions/ReviewDelta"
      }
    },
    "fileKey": {
      "type": "string"
    },
    "frameDigest": {
      "description": "The frame's content digest at review time, as the frames ledger computes it. The frame side's input hash.",
      "type": "string"
    },
    "frameImageDigest": {
      "description": "Hash of the exported frame image the reviewer saw.",
      "type": "string"
    },
    "id": {
      "$ref": "#/definitions/ReviewId"
    },
    "nodeId": {
      "description": "The frame reviewed against, in the `12:34` spelling.",
      "type": "string"
    },
    "notes": {
      "type": [
        "string",
        "null"
      ]
    },
    "regions": {
      "description": "Which bands of the pair were examined, and what was found in each.\n\nRequired non-empty. A verdict with no region checklist is a verdict about \"the page\", and a finding about the page cannot be told apart from a glance at the page.",
      "default": [],
      "type": "array",
      "items": {
        "$ref": "#/definitions/RegionFinding"
      }
    },
    "reviewedAt": {
      "$ref": "#/definitions/Timestamp"
    },
    "reviewedBy": {
      "description": "The reviewing agent's identity: model, harness, prompt version.",
      "type": "string"
    },
    "routes": {
      "description": "The route(s) this pass covered, as the screens ledger spells them.\n\nA LIST so a grouped verdict is representable and refusable by name. Read the module note: a single-route field would have made the family review that motivated this amendment impossible to write down and impossible to catch. Exactly one route confers coverage; more confers none.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "screenshotPath": {
      "description": "Where the screenshot IS, project-relative, so the hash above can be RE-COMPUTED rather than believed.\n\nA conform verdict with no path rests on two hashes of three, and the missing one is the picture of the product: nothing in the record can be re-derived from the artefact, so the whole parity claim is a field somebody typed. Three of the four earning conditions were found on the downstream estate by MINTING a passing record, and each one alone conjured a route at parity out of nothing.\n\n`Option` because a `deviate` verdict claims no parity and must not be held to a parity standard: hold it there and people stop recording deviations, which costs more than the field buys.",
      "type": [
        "string",
        "null"
      ]
    },
    "servedBuild": {
      "description": "The build the screenshot was taken OF, in the estate's own spelling: a commit, a tag, an image digest.\n\n[`Self::shipped_source_digest`] is computed from the LOCAL TREE, so on its own it can describe code the screenshot never ran: a reviewer captures a deployed page, digests the working copy, and the record links a picture of one build to the hash of another. Naming the served build is the only thing in the record that says which one was on the screen.",
      "type": [
        "string",
        "null"
      ]
    },
    "shippedScreenshotDigest": {
      "description": "Hash of the shipped screenshot artefact itself.",
      "type": "string"
    },
    "shippedSourceDigest": {
      "description": "The screens-ledger digest of the route's SOURCE at review time. The shipped side's input hash: when the route's source moves, this no longer matches the ledger and the verdict is STALE.",
      "type": "string"
    },
    "verdict": {
      "description": "The reviewer's verdict. `no_authority` is recordable - a pipeline that found the frame unsigned says so honestly - and the proof also DERIVES authority itself; a recorded `conform` against a frame the register shows unsigned is refused, not trusted.",
      "$ref": "#/definitions/AuthorityVerdict"
    }
  },
  "additionalProperties": false,
  "definitions": {
    "AuthorityVerdict": {
      "description": "The verdict vocabulary of every frame-bound proof. CLOSED at three.\n\nThere is deliberately no fourth variant for an accepted deviation. An acceptance state is taste exercised downstream of sign-off, which the constitutional direction forbids: the resolution path for a deviation is a new signed frame version, recorded as a [`RedrawRecord`].",
      "oneOf": [
        {
          "description": "The shipped artefact matches the signed frame.",
          "type": "string",
          "enum": [
            "conform"
          ]
        },
        {
          "description": "The shipped artefact differs from the signed frame. An ADDITION the frame omits is a deviation exactly like a missing element; direction carries no excuse.",
          "type": "string",
          "enum": [
            "deviate"
          ]
        },
        {
          "description": "The frame is unsigned, parked, or a proposal: there is no authority to conform to. Never green, never red; reported as coverage owed.",
          "type": "string",
          "enum": [
            "no_authority"
          ]
        }
      ]
    },
    "DeltaDisposition": {
      "description": "What a recorded difference IS, in the reviewer's own analysis. CLOSED.\n\nA bare delta list answered \"what differs\" and nothing else, so every difference read as one kind of thing: work owed by the code. On the estate this lane was written for, that is false of most of them. A frame drawn before a product decision, an annotation layer the exporter drew into the image, a shipped surface the design has since preferred, a band that needs data nobody seeded, chrome that belongs to the shell and not the route, and a difference a policy forbids closing are six different facts, and a list that cannot tell them apart routes all six to the same queue.\n\n# There is no `accepted` and no `wont_fix`, and there never will be\n\nAn acceptance state is TASTE, and taste is exercised once, at frame sign-off ([2026] VJS-SC-OPBOX 1, and the repeal recorded in [`crate::AuthorityVerdict`]). A fourth value here would be a fourth [`AuthorityVerdict`] wearing a different field name, reachable by the recorder rather than by the signer, and it would be the whole repeal undone one enum away from the one that carries it.\n\nSo EVERY disposition leaves the verdict `deviate` and leaves the deviation rule RED. A disposition CLASSIFIES and never DISPOSES: it says what kind of work the difference is, and the three routes out of a deviation are unchanged (a covering sign-off, a registered direction, or a deletion that independently discharges [2026] VJS-CC-OPBOX 155 O7).",
      "oneOf": [
        {
          "description": "The code does not draw what the signed frame draws. The default kind, and the only one that needs no citation: it is a claim about the two artefacts the verdict already names.",
          "type": "string",
          "enum": [
            "code_defect"
          ]
        },
        {
          "description": "The frame is behind the product: the shipped surface implements a later decision the drawing has not caught up with. Owed by a redraw.",
          "type": "string",
          "enum": [
            "frame_behind_product"
          ]
        },
        {
          "description": "The difference is in the FRAME IMAGE and not in the design: a redline, a spec callout, a comment pin the export drew. Owed by a redraw, because the cure is a clean frame and not a code change.",
          "type": "string",
          "enum": [
            "frame_draws_annotation"
          ]
        },
        {
          "description": "The shipped surface is preferred to the drawing. Still a deviation, and still red: the resolution is a redraw adopting it, never this field.",
          "type": "string",
          "enum": [
            "shipped_is_better"
          ]
        },
        {
          "description": "The band could not be compared because the capture had no data in it. Needs no citation: it is a fact about the capture the verdict names.",
          "type": "string",
          "enum": [
            "needs_absent_data"
          ]
        },
        {
          "description": "The difference is drawn by the application shell rather than by this route, so the subject of the finding is another screen. Owed by the SCREEN record that does own it.",
          "type": "string",
          "enum": [
            "belongs_to_app_chrome"
          ]
        },
        {
          "description": "Closing the difference is forbidden: a registered prohibition or a Principal direction stands in the way. Owed by that row.",
          "type": "string",
          "enum": [
            "forbidden_by_policy"
          ]
        }
      ]
    },
    "RegionFinding": {
      "description": "What the reviewer found in one region, or that they did not look.",
      "type": "object",
      "required": [
        "examined",
        "finding",
        "region"
      ],
      "properties": {
        "examined": {
          "description": "Whether this region of the PAIR was actually compared.\n\n`false` is a first-class, recordable answer and is the whole point: \"capture debt, needs seeded data\" is a legitimate thing to conclude about a region, and it has to be readable as a region that was not studied rather than disappearing into a verdict about the page.",
          "type": "boolean"
        },
        "finding": {
          "description": "The finding, or the reason the region was not examined. Required either way: a region row carrying neither is a row that says nothing.",
          "type": "string"
        },
        "region": {
          "$ref": "#/definitions/ReviewRegion"
        }
      },
      "additionalProperties": false
    },
    "ReviewDelta": {
      "description": "One recorded difference: what it is, what KIND of thing it is, and the row that carries the work where the kind asserts one exists.",
      "type": "object",
      "required": [
        "describes",
        "disposition"
      ],
      "properties": {
        "describes": {
          "description": "The difference, in the reviewer's own words.\n\nProse, and NOT exempt from VDS S-2(2): `.vds/**` is scanned in full by `no_stored_values` and the two ignored directories are closed at two (VDS S-3(9)), so a delta that spells a length, a duration or a colour literal puts that value under the record permanently. Name the CLASS - \"the row gutter is tighter than the frame draws\", \"the border reads at a lower contrast than the floor\" - exactly as every note this engine writes does.",
          "type": "string"
        },
        "disposition": {
          "description": "What kind of difference this is. Required, and a non-`Option` field under `deny_unknown_fields`, so a record that omits it is refused by the DESERIALISER rather than by a rule: an unclassified delta is not a weaker delta, it is the old bare string back.",
          "$ref": "#/definitions/DeltaDisposition"
        },
        "owedBy": {
          "description": "The row that carries the work, where the disposition asserts one exists. See [`DeltaDisposition::owed_by_prefixes`].",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "ReviewId": {
      "description": "A visual review verdict: automated eyes over a shipped screen against its signed frame (draft S-7D).",
      "type": "string",
      "pattern": "^VRW-[0-9]{4}$"
    },
    "ReviewRegion": {
      "description": "The bands of a screen a visual pass is expected to account for. CLOSED.\n\nClosed for the reason [`super::SurfaceKind`] is: an open set of region names lets one undifferentiated \"the page\" bucket back in, and \"the page looks fine\" is exactly the finding that missed a counted facet row, a counted eyebrow, date-group bands and a footer window statement on one route.\n\nThese are ANATOMY names and hold no design value (VDS S-2(4)): a region is a place to look, never a size, a colour or a spacing.",
      "oneOf": [
        {
          "description": "The page header band: title, eyebrow, counts, primary actions.",
          "type": "string",
          "enum": [
            "header"
          ]
        },
        {
          "description": "The filter/facet band, and whatever counts it carries.",
          "type": "string",
          "enum": [
            "facets"
          ]
        },
        {
          "description": "The body rows or cards, and their internal anatomy.",
          "type": "string",
          "enum": [
            "body_rows"
          ]
        },
        {
          "description": "The side rail, where the frame draws one.",
          "type": "string",
          "enum": [
            "rail"
          ]
        },
        {
          "description": "The footer band, and any window or scope statement in it.",
          "type": "string",
          "enum": [
            "footer"
          ]
        },
        {
          "description": "The empty state, which is a drawn state and not an absence.",
          "type": "string",
          "enum": [
            "empty_state"
          ]
        },
        {
          "description": "Keyboard affordances drawn in the frame: focus rings, hints, shortcuts.",
          "type": "string",
          "enum": [
            "keyboard"
          ]
        }
      ]
    },
    "SignoffId": {
      "description": "A frame sign-off: the frame's content hash at the moment taste was exercised (draft S-7D).",
      "type": "string",
      "pattern": "^SGN-[0-9]{4}$"
    },
    "Timestamp": {
      "description": "UTC, second resolution, trailing Z. The one canonical form VDS writes.",
      "type": "string",
      "format": "date-time",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$"
    }
  }
}
