{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "title": "StagePlan",
  "description": "The operation list, on disk, BEFORE anything reaches the canvas.\n\nThe genuinely new thing this whole capability buys. It lives in the SUBSCRIBER TREE beside the intent, because its operations carry boxes and resolved paints.",
  "type": "object",
  "required": [
    "chunks",
    "container",
    "contentDigest",
    "emittedAt",
    "emittedBy",
    "fileKey",
    "intentDigest",
    "nodeId",
    "reading",
    "readingDigest",
    "route",
    "schemaVersion",
    "stage"
  ],
  "properties": {
    "chunks": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/PlanChunk"
      }
    },
    "container": {
      "description": "The exact subtree all operations below are scoped to. For an unlabelled frame this is the frame itself; for a named current-source layer this is that layer. A plan without this fact leaves a bridge to resolve a band name against the wrong sibling.",
      "$ref": "#/definitions/StageContainer"
    },
    "contentDigest": {
      "type": "string"
    },
    "coverage": {
      "description": "The tally over [`Self::gates`], on the face of the artefact.\n\nStored AND held to its own recomputation by [`Self::untrustworthy_because`], for the reason [`Self::content_digest`] is stored: a reviewer reads the file, not this type's methods, and a summary that can be edited away from the facts beside it is a summary that lies at exactly the moment it matters.",
      "default": "",
      "type": "string"
    },
    "emittedAt": {
      "$ref": "#/definitions/Timestamp"
    },
    "emittedBy": {
      "type": "string"
    },
    "fileKey": {
      "type": "string"
    },
    "gates": {
      "description": "THE THREE-VALUED GATE READINGS THIS PLAN WAS EMITTED UNDER, one per gate that was asked.\n\nThis artefact is the one the design repeatedly calls THE REVIEWABLE ARTEFACT, and it carried no reading from any gate at all: a reviewer holding a plan could see the operations and could not see whether a single gate had run over them. Since `vds stage plan` refuses to emit against a refusal, every reading here is `cleared` or `could_not_run`, and telling those two apart is the whole reason the vocabulary is three-valued. A gate MISSING from this list was never asked, which [`Self::coverage`] states rather than leaving to a reader who might not know how many gates there are.",
      "default": [],
      "type": "array",
      "items": {
        "$ref": "#/definitions/GateVerdict"
      }
    },
    "intentDigest": {
      "type": "string"
    },
    "nodeId": {
      "type": "string"
    },
    "reading": {
      "description": "The saved capture the diff was taken against, project-relative.",
      "type": "string"
    },
    "readingDigest": {
      "description": "That capture's digest, so a plan cannot be applied against a frame reading it was never computed from.",
      "type": "string"
    },
    "route": {
      "type": "string"
    },
    "schemaVersion": {
      "type": "integer",
      "format": "uint32",
      "minimum": 0.0
    },
    "stage": {
      "$ref": "#/definitions/StageId"
    },
    "untouched": {
      "description": "The bands present in the reading whose names the closed vocabulary does NOT name. Recorded and never touched: a node VDS did not create and the intent does not name is out of reach of every operation above.",
      "default": [],
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  },
  "additionalProperties": false,
  "definitions": {
    "BandBox": {
      "description": "A rectangle, frame-relative.\n\nA REALISATION, and it is lawful here for exactly one reason: this type is only ever reached from [`StageIntent`] and [`StagePlan`], both of which live in the SUBSCRIBER TREE. Nothing under `.vds/**` holds one, and the stage record's own test holds that claim against its serialised bytes rather than against this paragraph.",
      "type": "object",
      "required": [
        "height",
        "width",
        "x",
        "y"
      ],
      "properties": {
        "height": {
          "type": "number",
          "format": "double"
        },
        "width": {
          "type": "number",
          "format": "double"
        },
        "x": {
          "type": "number",
          "format": "double"
        },
        "y": {
          "type": "number",
          "format": "double"
        }
      },
      "additionalProperties": false
    },
    "GateReading": {
      "description": "What one gate READ. THREE-VALUED, and the third value is the whole point.\n\n\"Cleared\" and \"had no basis to run\" are different facts, and one field holding both reports the second as the first. On the estate this was written for, the same class of collapse reported 143 unmeasured routes as clean, and `screen_parity`'s coverage tally exists because a gate scored 32% of its subject and would have printed zero deviations.",
      "oneOf": [
        {
          "description": "The gate ran, over something, and found nothing to refuse.",
          "type": "string",
          "enum": [
            "cleared"
          ]
        },
        {
          "description": "The gate ran and refuses this staged write.",
          "type": "string",
          "enum": [
            "refused"
          ]
        },
        {
          "description": "The gate had no basis to run: an input the estate has not supplied, or a declaration this intent does not make. NEVER a pass.",
          "type": "string",
          "enum": [
            "could_not_run"
          ]
        }
      ]
    },
    "GateVerdict": {
      "description": "One gate's reading on one staged write, and why.",
      "type": "object",
      "required": [
        "because",
        "gate",
        "reading"
      ],
      "properties": {
        "because": {
          "description": "The reason, in the gate's own words, and required in EVERY reading.\n\nA cleared reading with no sentence cannot be told apart from a gate that returned early, and a `could_not_run` with no sentence hides which input was missing. Prose, and NOT exempt from VDS S-2(2): this record lands under `.vds/**`, which `no_stored_values` scans in full, so a reason names the CLASS of a value and never spells one.",
          "type": "string"
        },
        "gate": {
          "$ref": "#/definitions/StageGate"
        },
        "reading": {
          "$ref": "#/definitions/GateReading"
        }
      },
      "additionalProperties": false
    },
    "PlanChunk": {
      "description": "One chunk of a plan, digest-pinned.",
      "type": "object",
      "required": [
        "digest",
        "operations",
        "ordinal"
      ],
      "properties": {
        "digest": {
          "description": "A digest over this chunk's operations, so a chunk cannot be reordered, dropped or edited between emission and apply without saying so.",
          "type": "string"
        },
        "operations": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/StageOperation"
          }
        },
        "ordinal": {
          "description": "Position in the ordered sequence, from 1. The order is part of the plan: a create must precede the set-box that positions what it created.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "ReviewRegion": {
      "description": "The bands of a screen a visual pass is expected to account for. CLOSED.\n\nClosed for the reason [`super::SurfaceKind`] is: an open set of region names lets one undifferentiated \"the page\" bucket back in, and \"the page looks fine\" is exactly the finding that missed a counted facet row, a counted eyebrow, date-group bands and a footer window statement on one route.\n\nThese are ANATOMY names and hold no design value (VDS S-2(4)): a region is a place to look, never a size, a colour or a spacing.",
      "oneOf": [
        {
          "description": "The page header band: title, eyebrow, counts, primary actions.",
          "type": "string",
          "enum": [
            "header"
          ]
        },
        {
          "description": "The filter/facet band, and whatever counts it carries.",
          "type": "string",
          "enum": [
            "facets"
          ]
        },
        {
          "description": "The body rows or cards, and their internal anatomy.",
          "type": "string",
          "enum": [
            "body_rows"
          ]
        },
        {
          "description": "The side rail, where the frame draws one.",
          "type": "string",
          "enum": [
            "rail"
          ]
        },
        {
          "description": "The footer band, and any window or scope statement in it.",
          "type": "string",
          "enum": [
            "footer"
          ]
        },
        {
          "description": "The empty state, which is a drawn state and not an absence.",
          "type": "string",
          "enum": [
            "empty_state"
          ]
        },
        {
          "description": "Keyboard affordances drawn in the frame: focus rings, hints, shortcuts.",
          "type": "string",
          "enum": [
            "keyboard"
          ]
        }
      ]
    },
    "StageContainer": {
      "description": "The exact subtree a staged operation is allowed to touch.\n\nA band name is an identity key, not a parent selector. A frame may carry a legacy sibling with a band of the same name beside its current source layer; applying `delete-band header` without this scope would leave the bridge free to choose the wrong one. The node id therefore travels with every operation and with the plan that carries it. The name is for a reviewer; the id is the binding that makes the scope unambiguous.",
      "type": "object",
      "required": [
        "name",
        "nodeId"
      ],
      "properties": {
        "name": {
          "type": "string"
        },
        "nodeId": {
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "StageGate": {
      "description": "The four gates a staged write is read against. CLOSED.\n\nClosed for the reason [`ReviewRegion`] is: an open set lets one undifferentiated \"the checks passed\" bucket back in, and a gate nobody can name is a gate nobody can see did not run.\n\nAll four are limbs of ONE proof kind, `staged_write`, and never four kinds. Four kinds would walk the same enumeration four times and let the four disagree about which stages exist, which is the two-sources-of-truth failure `visual_review` names when it folds band correspondence in rather than filing a sixteenth kind for it.",
      "oneOf": [
        {
          "description": "G1. A staged control boundary clears the contrast floor in every theme the shipped stylesheet declares.",
          "type": "string",
          "enum": [
            "contrast_floor"
          ]
        },
        {
          "description": "G2. Every staged band is named from the CLOSED review vocabulary and is one the screen record declares. The limb that makes the diff key stable.",
          "type": "string",
          "enum": [
            "band_naming"
          ]
        },
        {
          "description": "G3. The boxes about to be written derive the column count the intent declares, under the same derivation `vds figma frames` runs afterwards.",
          "type": "string",
          "enum": [
            "canonical_geometry"
          ]
        },
        {
          "description": "G4. No other artefact in the subscribing repository contradicts this route's binding to this frame.",
          "type": "string",
          "enum": [
            "route_binding"
          ]
        }
      ]
    },
    "StageId": {
      "description": "A staged write to one frame: the reviewable record of what was intended, which gates read on it, and what an apply then did (draft S-7E).",
      "type": "string",
      "pattern": "^STG-[0-9]{4}$"
    },
    "StageOperation": {
      "description": "What a staged apply may do. CLOSED AT SIX, and the closure is the strongest thing in this module.\n\nTHERE IS NO PAGE-LEVEL AND NO FRAME-LEVEL DELETE, AND THERE MUST NEVER BE. The 2026-07-25 loss on the subscribing estate came from a build step whose correct and documented behaviour is to delete a page of a given name and recreate it; a second writer ran it and discarded work the first had landed. Neither agent was at fault and the step was not wrong: the destructive verb simply existed and two writers reached it. Widening this enum re-creates that verb inside the sanctioned path, and the loss becomes repeatable through the route that was built to prevent it. That is why the reason is written here in the code and not only in a design note.\n\n[`StageOperation::DeleteBand`] is admitted under two conditions checked together and never separately: the band's name is in the closed review vocabulary, AND the intent NAMES IT IN [`StageIntent::deletes`]. A node VDS did not create and the intent does not name is never touched at all.\n\nTHAT SECOND CONDITION USED TO BE THE INTENT'S SILENCE, and the difference is not a nicety. A delete was emitted for every closed-vocabulary band in the frame the intent did not declare, so an intent about the header deleted a `facets` band a designer had drawn, purely by not mentioning it. No gate read the canvas, so the one destructive verb in the vocabulary was the only operation emitted with no reading behind it at all. It is now the only verb that requires the intent to say the word.",
      "oneOf": [
        {
          "description": "The intent declares a band the frame does not have.",
          "type": "object",
          "required": [
            "band",
            "box_of",
            "container",
            "op"
          ],
          "properties": {
            "band": {
              "$ref": "#/definitions/ReviewRegion"
            },
            "box_of": {
              "$ref": "#/definitions/BandBox"
            },
            "container": {
              "$ref": "#/definitions/StageContainer"
            },
            "op": {
              "type": "string",
              "enum": [
                "create_band"
              ]
            }
          },
          "additionalProperties": false
        },
        {
          "description": "The frame has the band and its rectangle is not the declared one.",
          "type": "object",
          "required": [
            "band",
            "box_of",
            "container",
            "op"
          ],
          "properties": {
            "band": {
              "$ref": "#/definitions/ReviewRegion"
            },
            "box_of": {
              "$ref": "#/definitions/BandBox"
            },
            "container": {
              "$ref": "#/definitions/StageContainer"
            },
            "op": {
              "type": "string",
              "enum": [
                "set_box"
              ]
            }
          },
          "additionalProperties": false
        },
        {
          "description": "The band's layer name is a variant spelling of its own region.\n\nThe band's IDENTITY is the region parsed from its layer name, so this operation moves a spelling onto the canonical one and is idempotent by construction: once the name is canonical it parses to the same region and the comparison is equal.",
          "type": "object",
          "required": [
            "band",
            "container",
            "op",
            "to"
          ],
          "properties": {
            "band": {
              "$ref": "#/definitions/ReviewRegion"
            },
            "container": {
              "$ref": "#/definitions/StageContainer"
            },
            "op": {
              "type": "string",
              "enum": [
                "set_name"
              ]
            },
            "to": {
              "type": "string"
            }
          },
          "additionalProperties": false
        },
        {
          "description": "The band's paint is not the one the named custom property resolves to.",
          "type": "object",
          "required": [
            "band",
            "container",
            "op",
            "property",
            "resolved"
          ],
          "properties": {
            "band": {
              "$ref": "#/definitions/ReviewRegion"
            },
            "container": {
              "$ref": "#/definitions/StageContainer"
            },
            "op": {
              "type": "string",
              "enum": [
                "set_paint"
              ]
            },
            "property": {
              "description": "The custom property the paint takes its value from. The NAME travels with the operation so a reader of the plan can see which token was staged, not only what it happened to resolve to.",
              "type": "string"
            },
            "resolved": {
              "description": "What that property resolves to in the base scope of the shipped stylesheet. A realisation, lawful because a plan lives in the subscriber tree.",
              "type": "string"
            }
          },
          "additionalProperties": false
        },
        {
          "description": "The band sits at a different index among the frame's bands.",
          "type": "object",
          "required": [
            "band",
            "container",
            "op",
            "to"
          ],
          "properties": {
            "band": {
              "$ref": "#/definitions/ReviewRegion"
            },
            "container": {
              "$ref": "#/definitions/StageContainer"
            },
            "op": {
              "type": "string",
              "enum": [
                "reorder"
              ]
            },
            "to": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            }
          },
          "additionalProperties": false
        },
        {
          "description": "The frame carries a band whose name IS in the closed vocabulary and which the intent EXPLICITLY LISTS in `deletes`. The only destructive verb, and its reach is one band.\n\nIt is emitted for a listed band and never for an unmentioned one: the intent's silence about a band is not permission to remove it.",
          "type": "object",
          "required": [
            "band",
            "container",
            "op"
          ],
          "properties": {
            "band": {
              "$ref": "#/definitions/ReviewRegion"
            },
            "container": {
              "$ref": "#/definitions/StageContainer"
            },
            "op": {
              "type": "string",
              "enum": [
                "delete_band"
              ]
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "Timestamp": {
      "description": "UTC, second resolution, trailing Z. The one canonical form VDS writes.",
      "type": "string",
      "format": "date-time",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$"
    }
  }
}
