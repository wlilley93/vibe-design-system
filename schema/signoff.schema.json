{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "title": "SignOff",
  "description": "One frame sign-off: the frame's content hash at the moment it was signed.",
  "type": "object",
  "required": [
    "fileKey",
    "frameDigest",
    "id",
    "nodeId",
    "signedAt",
    "signedBy"
  ],
  "properties": {
    "evidence": {
      "description": "The external act imported by this row, where `signed_at` records an event that happened before this command ran.\n\nOptional so every sign-off recorded through the existing live door keeps exactly its established shape. External time is different: it is accepted only with a durable, repository-local evidence binding whose bytes can be checked again whenever the register is read.",
      "anyOf": [
        {
          "$ref": "#/definitions/SignOffEvidence"
        },
        {
          "type": "null"
        }
      ]
    },
    "fileKey": {
      "type": "string"
    },
    "frameDigest": {
      "description": "The frame's content digest AT SIGN-OFF, as the frames ledger computes it. The whole mechanism: authority holds while the current digest equals this one, and not a moment longer.",
      "type": "string"
    },
    "id": {
      "$ref": "#/definitions/SignoffId"
    },
    "nodeId": {
      "description": "The frame's node id, in the `12:34` spelling.",
      "type": "string"
    },
    "notes": {
      "type": [
        "string",
        "null"
      ]
    },
    "signedAt": {
      "$ref": "#/definitions/Timestamp"
    },
    "signedBy": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "definitions": {
    "SignOffEvidence": {
      "description": "A durable reference from one imported sign-off row to its external act.\n\n`digest` identifies the exact evidence bytes. `frame_ledger_digest` records the aggregate the act signed and lets a later reader validate the act without needing the Figma/frames crate. The path is repository-relative so the reference survives moving or cloning the project.",
      "type": "object",
      "required": [
        "digest",
        "frameLedgerDigest",
        "path"
      ],
      "properties": {
        "digest": {
          "type": "string"
        },
        "frameLedgerDigest": {
          "type": "string"
        },
        "path": {
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "SignoffId": {
      "description": "A frame sign-off: the frame's content hash at the moment taste was exercised (draft S-7D).",
      "type": "string",
      "pattern": "^SGN-[0-9]{4}$"
    },
    "Timestamp": {
      "description": "UTC, second resolution, trailing Z. The one canonical form VDS writes.",
      "type": "string",
      "format": "date-time",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$"
    }
  }
}
