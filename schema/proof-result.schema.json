{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "title": "ProofResult",
  "description": "The machine output a warrant is granted against.",
  "type": "object",
  "required": [
    "capture_mode",
    "captured_at",
    "command",
    "designpack_digest",
    "digest",
    "exit_code",
    "id",
    "inputs_digest",
    "kind",
    "rows_considered",
    "rows_enforced",
    "script",
    "status",
    "violations"
  ],
  "properties": {
    "capture_mode": {
      "$ref": "#/definitions/CaptureMode"
    },
    "captured_at": {
      "$ref": "#/definitions/Timestamp"
    },
    "command": {
      "description": "The exact command that produced it, so VDS S-7(2)(1) is checkable.",
      "type": "string"
    },
    "designpack_digest": {
      "type": "string"
    },
    "digest": {
      "description": "The digest a warrant cites. Covers the run's FINDINGS and inputs, and deliberately not its timing, so an unchanged input re-run cites the same evidence (VDS S-7(2)(1)).",
      "type": "string"
    },
    "duration_ms": {
      "type": [
        "integer",
        "null"
      ],
      "format": "uint64",
      "minimum": 0.0
    },
    "exit_code": {
      "type": "integer",
      "format": "int32"
    },
    "id": {
      "$ref": "#/definitions/ProofId"
    },
    "inputs_digest": {
      "type": "string"
    },
    "invoked_by": {
      "default": "manual",
      "$ref": "#/definitions/InvokedBy"
    },
    "kind": {
      "$ref": "#/definitions/ProofKind"
    },
    "notes": {
      "description": "Lines the run printed that are not violations: reliance on a reserved clause, a deprecated consumer, a carve-out that was taken.",
      "default": [],
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "rows_considered": {
      "type": "integer",
      "format": "uint64",
      "minimum": 0.0
    },
    "rows_enforced": {
      "type": "integer",
      "format": "uint64",
      "minimum": 0.0
    },
    "rows_skipped_reasons": {
      "description": "Count per reason a row was considered but not enforced, so a vacuous pass is diagnosable rather than merely flagged.",
      "default": {},
      "type": "object",
      "additionalProperties": {
        "type": "integer",
        "format": "uint64",
        "minimum": 0.0
      }
    },
    "script": {
      "description": "The gate that ran, repository-relative.",
      "type": "string"
    },
    "script_digest": {
      "type": [
        "string",
        "null"
      ]
    },
    "status": {
      "$ref": "#/definitions/ProofStatus"
    },
    "violations": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/Violation"
      }
    },
    "warrant_id": {
      "description": "Set when a warrant cites this record. Null until then.",
      "default": null,
      "anyOf": [
        {
          "$ref": "#/definitions/WarrantId"
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "additionalProperties": false,
  "definitions": {
    "CaptureMode": {
      "description": "VDS S-7(2)(5): a hand-written proof record is void, and the contract enforces it by admitting exactly one value.",
      "type": "string",
      "enum": [
        "automatic"
      ]
    },
    "Drift": {
      "description": "WHICH SIDE MOVED, where the direction is knowable.\n\nA finding that says only `expected` and `actual` names a disagreement and not a JOB. \"The contract requires a prop the code lacks\" and \"the code accepts a prop no contract names\" are the same disagreement pointing opposite ways, and they are owed by different people: the first is usually an implementation, the second is usually an AMENDMENT. Reading a wall of findings and sorting them by hand is work the proof already did and threw away.\n\nBorrowed, with attribution, from `southleft/ds-contracts-poc` (`parity/diff.ts`), where every finding carries `classification: ahead | behind | mismatch` against a surface. Their model is always contract-versus-surface and never surface-to-surface, which is the half that makes the direction meaningful, and it is the same discipline VDS already applies by making the register the record.\n\nDerivable here WITHOUT history, which is why it can be added at all: the direction follows from which side is missing the thing, not from what changed. `Undetermined` is the honest default and is what every finding carries until its emitter has an opinion - guessing a direction would be worse than leaving it blank, because a wrong direction sends the work to the wrong person with a proof's authority behind it.",
      "oneOf": [
        {
          "description": "The surface carries something the contract does not name. An amendment is usually owed, not a fix.",
          "type": "string",
          "enum": [
            "ahead"
          ]
        },
        {
          "description": "The contract requires something the surface does not have. An implementation is usually owed.",
          "type": "string",
          "enum": [
            "behind"
          ]
        },
        {
          "description": "Both sides name it and they disagree about it.",
          "type": "string",
          "enum": [
            "mismatch"
          ]
        },
        {
          "description": "This emitter has no opinion about the direction, and says so rather than choosing one.",
          "type": "string",
          "enum": [
            "undetermined"
          ]
        }
      ]
    },
    "InvokedBy": {
      "description": "What invoked the run. VDS S-7(2)(3) requires something other than the author choosing to run it, and VDS S-7(3) holds that a hook is not CI.",
      "type": "string",
      "enum": [
        "githook_pre_commit",
        "githook_pre_push",
        "ci_workflow",
        "package_script",
        "build",
        "manual"
      ]
    },
    "ProofId": {
      "description": "A captured proof result, stamped with the UTC second it was captured.",
      "type": "string",
      "pattern": "^PROOF-[0-9]{8}-[0-9]{6}$"
    },
    "ProofKind": {
      "description": "The closed registry at VDS S-7(5).",
      "oneOf": [
        {
          "description": "Every component referenced by any declared screen exists in the register.",
          "type": "string",
          "enum": [
            "register_completeness"
          ]
        },
        {
          "description": "The register agrees with the codebase and with Figma, both directions.",
          "type": "string",
          "enum": [
            "reconciliation"
          ]
        },
        {
          "description": "No screen uses an unregistered component. The anti-drift proof.",
          "type": "string",
          "enum": [
            "composition"
          ]
        },
        {
          "description": "Every registered component's boundaries clear their floors in every theme.",
          "type": "string",
          "enum": [
            "contrast"
          ]
        },
        {
          "description": "Every required state of every registered component is drawn.",
          "type": "string",
          "enum": [
            "states"
          ]
        },
        {
          "description": "Each registered component's code counterpart matches its contract.",
          "type": "string",
          "enum": [
            "parity"
          ]
        },
        {
          "description": "The two named records agree where the pin declares them aligned.",
          "type": "string",
          "enum": [
            "token_pin"
          ]
        },
        {
          "description": "A component proposed for retirement has zero remaining consumers.",
          "type": "string",
          "enum": [
            "retirement_drain"
          ]
        },
        {
          "description": "Each generated ledger is current with its source.",
          "type": "string",
          "enum": [
            "ledger_staleness"
          ]
        },
        {
          "description": "`.vds/**` holds no realisation.",
          "type": "string",
          "enum": [
            "no_stored_values"
          ]
        },
        {
          "description": "Each registered screen's required arrangement is the arrangement its authoritative frame draws. The only kind whose subject is a SCREEN.",
          "type": "string",
          "enum": [
            "screen_parity"
          ]
        },
        {
          "description": "Each registered surface's SHAPE is the one the design system specifies, and the count that does not comply is BOUNDED AND FALLING. The only kind that carries a DIRECTION rather than a threshold.",
          "type": "string",
          "enum": [
            "geometry"
          ]
        },
        {
          "description": "A registered pattern is ABSENT from its enumerated scope, and the scope cannot silently narrow. Draft S-7B, enactment pending.",
          "type": "string",
          "enum": [
            "prohibition"
          ]
        },
        {
          "description": "A pinned numeric reading whose only lawful direction is down: red on any increase AND on a decrease that was not re-pinned. Draft S-7C, enactment pending.",
          "type": "string",
          "enum": [
            "burndown"
          ]
        },
        {
          "description": "The recorded visual verdicts hold: shipped screenshot against signed frame, stale on either side moving, no conformance claim without authority. Draft S-7D, enactment pending.",
          "type": "string",
          "enum": [
            "visual_review"
          ]
        },
        {
          "description": "Every UNAPPLIED staged write clears its four gates, and every frame the estate names carries a content digest something VDS holds accounts for. Draft S-7E, enactment pending.\n\nONE kind and never four, though it carries four gates. Four kinds would walk the same enumeration four times and let the four disagree about which stages exist, which is the two-sources-of-truth failure `visual_review` names when it folds the band correspondence in rather than filing a separate kind for it.",
          "type": "string",
          "enum": [
            "staged_write"
          ]
        }
      ]
    },
    "ProofStatus": {
      "description": "The three outcomes a proof record may carry.\n\nThere is no \"skipped\" and no \"warning\". VDS S-7(2)(4) requires a pass over zero enforceable rows to be recorded as `vacuous` and never as `passed`, and the absence of a fourth variant is what makes that structural.",
      "type": "string",
      "enum": [
        "passed",
        "failed",
        "vacuous"
      ]
    },
    "Severity": {
      "type": "string",
      "enum": [
        "fatal",
        "warning",
        "informational"
      ]
    },
    "Timestamp": {
      "description": "UTC, second resolution, trailing Z. The one canonical form VDS writes.",
      "type": "string",
      "format": "date-time",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$"
    },
    "Violation": {
      "description": "One finding. `expected` and `actual` are both required and both non-empty, because a finding that says only what is wrong makes the reader guess at what right would have been.",
      "type": "object",
      "required": [
        "actual",
        "expected",
        "location",
        "rule"
      ],
      "properties": {
        "actual": {
          "type": "string"
        },
        "drift": {
          "description": "Which side moved. `#[serde(default)]` so every proof record already on disk still parses, and reads as `undetermined` - which is true of them.",
          "default": "undetermined",
          "$ref": "#/definitions/Drift"
        },
        "expected": {
          "type": "string"
        },
        "location": {
          "description": "`file:line`, or a route plus a component id.",
          "type": "string"
        },
        "rule": {
          "description": "The invariant id or statute section the violation offends.",
          "type": "string"
        },
        "severity": {
          "default": "fatal",
          "$ref": "#/definitions/Severity"
        }
      },
      "additionalProperties": false
    },
    "WarrantId": {
      "description": "A warrant record. The stage number is part of the identifier so the four series are independent.",
      "type": "string",
      "pattern": "^WARRANT-W[1-4]-[0-9]{3}$"
    }
  }
}
