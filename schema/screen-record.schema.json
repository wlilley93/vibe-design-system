{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "title": "ScreenRecord",
  "description": "One registered screen.",
  "type": "object",
  "required": [
    "arrangement",
    "basis",
    "contractVersion",
    "id",
    "route",
    "status"
  ],
  "properties": {
    "arrangement": {
      "$ref": "#/definitions/ArrangementContract"
    },
    "basis": {
      "description": "The authorities this registration rests on.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "contractVersion": {
      "description": "Bumped on every amendment, so a contract change is a versioned event and never a silent edit (VDS S-9(2)).",
      "type": "integer",
      "format": "uint32",
      "minimum": 0.0
    },
    "frame": {
      "description": "The frame in the decided-target Figma file that DRAWS this screen. Null while the screen is proposed and nothing has been drawn; a proof reports the absence rather than assuming it away.",
      "anyOf": [
        {
          "$ref": "#/definitions/FigmaFrame"
        },
        {
          "type": "null"
        }
      ]
    },
    "id": {
      "$ref": "#/definitions/ScreenId"
    },
    "notes": {
      "type": [
        "string",
        "null"
      ]
    },
    "route": {
      "description": "What the screen IS, in the project's own vocabulary: a route, a path, a name. Compared to nothing by VDS and printed in every finding, because a finding that names only `SCR-0007` makes the reader go looking.",
      "type": "string"
    },
    "status": {
      "$ref": "#/definitions/Status"
    }
  },
  "additionalProperties": false,
  "definitions": {
    "Accessibility": {
      "type": "object",
      "required": [
        "accessibleNameSource",
        "contrastFloors",
        "keyboard"
      ],
      "properties": {
        "accessibleNameSource": {
          "$ref": "#/definitions/NameSource"
        },
        "contrastFloors": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/ContrastFloor"
          }
        },
        "keyboard": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/KeyboardContract"
          }
        },
        "role": {
          "description": "ARIA role, or null where the component is a layout container with none.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "Amendment": {
      "type": "object",
      "required": [
        "at",
        "by",
        "contractVersion",
        "kind",
        "what"
      ],
      "properties": {
        "at": {
          "$ref": "#/definitions/Timestamp"
        },
        "by": {
          "type": "string"
        },
        "contractVersion": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "decisionLogId": {
          "type": [
            "string",
            "null"
          ]
        },
        "kind": {
          "$ref": "#/definitions/AmendmentKind"
        },
        "proofId": {
          "anyOf": [
            {
              "$ref": "#/definitions/ProofId"
            },
            {
              "type": "null"
            }
          ]
        },
        "warrantId": {
          "anyOf": [
            {
              "$ref": "#/definitions/WarrantId"
            },
            {
              "type": "null"
            }
          ]
        },
        "what": {
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "AmendmentKind": {
      "type": "string",
      "enum": [
        "non_breaking",
        "breaking"
      ]
    },
    "ArrangementContract": {
      "description": "What arrangement this screen requires. A requirement, never a realisation.",
      "type": "object",
      "required": [
        "columns"
      ],
      "properties": {
        "columns": {
          "description": "How many side-by-side content PANES the screen requires, left to right.\n\nA count, not a width. See the module note: a width is a realisation and has nowhere to live here.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "regions": {
          "description": "The named regions the screen requires, in the project's own vocabulary (`rail`, `cmdbar`, `body`, `statusbar` in the subject this was derived from).\n\nFree strings and not an enum, because the region vocabulary is the SUBJECT's and not VDS's. A closed set here would be VDS deciding what a screen is made of, which is the fourth authority [2026] VJS-CC-OPBOX 3 forbids. The generator is told which names to look for through `[screens] region_names` in the project config, so the two halves read one list.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "CodeCounterpart": {
      "type": "object",
      "required": [
        "exportName",
        "importPath",
        "sourceFile"
      ],
      "properties": {
        "exportName": {
          "type": "string"
        },
        "importPath": {
          "description": "The module specifier a screen imports from, as written in the import.",
          "type": "string"
        },
        "sourceFile": {
          "description": "Repository-relative path, no leading slash.",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "ComponentId": {
      "description": "A registered component, allocated as the highest on disk plus one (VDS S-4(4)).",
      "type": "string",
      "pattern": "^CMP-[0-9]{4}$"
    },
    "ContrastFloor": {
      "description": "A contrast floor: a REQUIREMENT, never a realisation (VDS S-2(6)).\n\n`boundary` and `against` name tokens or edges BY NAME. A literal value in either is a storing-form violation of VDS S-2(2), and `no_stored_values` looks for exactly that.",
      "type": "object",
      "required": [
        "against",
        "basis",
        "boundary",
        "minRatio"
      ],
      "properties": {
        "against": {
          "type": "string"
        },
        "basis": {
          "description": "Where the requirement comes from, e.g. \"WCAG 2.2 SC 1.4.11\".",
          "type": "string"
        },
        "boundary": {
          "type": "string"
        },
        "minRatio": {
          "type": "number",
          "format": "double"
        },
        "scope": {
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/FloorScope"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "Demand": {
      "description": "How many routes consume this component. Measured, never estimated (VDS S-5(7)), which is why the command that measured it is part of the value.",
      "type": "object",
      "required": [
        "measuredAt",
        "measuredBy",
        "routes"
      ],
      "properties": {
        "measuredAt": {
          "$ref": "#/definitions/Timestamp"
        },
        "measuredBy": {
          "description": "The exact command that produced the count.",
          "type": "string"
        },
        "routes": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "FigmaFrame": {
      "description": "The frame a screen record is drawn by.\n\nDeliberately NOT [`super::FigmaNode`], even though the fields coincide today. A node is a component set and carries variants; a frame is a screen drawing and carries an authority layer. Sharing the type would make a screen record and a component record interchangeable at the seam where they are read, and the first field either one gains that the other must not have would then be an amendment to both.",
      "type": "object",
      "required": [
        "capturedAt",
        "fileKey",
        "nodeId"
      ],
      "properties": {
        "capturedAt": {
          "$ref": "#/definitions/Timestamp"
        },
        "fileKey": {
          "type": "string"
        },
        "nodeId": {
          "description": "A Figma node id, `<digits>:<digits>` in a file URL and `<digits>-<digits>` in a deep link. Both spellings are accepted because both are what a designer actually copies.",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "FigmaNode": {
      "type": "object",
      "required": [
        "capturedAt",
        "fileKey",
        "nodeId"
      ],
      "properties": {
        "capturedAt": {
          "$ref": "#/definitions/Timestamp"
        },
        "fileKey": {
          "type": "string"
        },
        "nodeId": {
          "description": "A Figma node id, which is `<digits>:<digits>` in a file URL and `<digits>-<digits>` in a deep link. Both spellings are accepted because both are what a designer actually copies.",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "FloorScope": {
      "description": "What kind of thing the boundary is.\n\nVDS S-9(5): where a lower floor is genuinely correct, the lawful move is to change the component's SCOPE and state the basis, not to loosen the ratio. A factual claim about scope is contestable by a reviewer; a quietly lowered floor is not. That is why this field exists at all.",
      "type": "string",
      "enum": [
        "control_boundary",
        "text",
        "graphical_object",
        "decoration"
      ]
    },
    "KeyboardContract": {
      "type": "object",
      "required": [
        "effect",
        "key"
      ],
      "properties": {
        "effect": {
          "type": "string"
        },
        "key": {
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "NameSource": {
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "children",
            "aria_label",
            "aria_labelledby",
            "title",
            "alt",
            "none_decorative"
          ]
        },
        {
          "description": "The HTML `<label>` association: a `for`/`id` pair, or a control wrapped by its label element.\n\nAdded because the set without it could not describe a text input, which is the single most common named control there is. `aria_labelledby` is a DIFFERENT mechanism, not a spelling of this one: it points at arbitrary elements by id and overrides the label association, so recording one where the code does the other publishes a contract the code does not keep. Found by adopting VDS on `examples/storefront`, where the only honest value for TextField was absent from the enum.",
          "type": "string",
          "enum": [
            "label"
          ]
        }
      ]
    },
    "ProofId": {
      "description": "A captured proof result, stamped with the UTC second it was captured.",
      "type": "string",
      "pattern": "^PROOF-[0-9]{8}-[0-9]{6}$"
    },
    "PropContract": {
      "type": "object",
      "required": [
        "name",
        "required",
        "type"
      ],
      "properties": {
        "figmaProperty": {
          "description": "The corresponding Figma variant property, or null where the prop has no visual counterpart.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "type": "string"
        },
        "required": {
          "type": "boolean"
        },
        "type": {
          "description": "The TypeScript type expression, or a closed union written `a|b|c`.\n\nSerialised as `type`, which is the field name the contract publishes and a reserved word in Rust. The rename keeps the published shape rather than letting an implementation-language constraint leak into the artefact.",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "ScreenId": {
      "description": "A registered screen: one route's ARRANGEMENT requirement, allocated as the highest on disk plus one (VDS S-4(4)).",
      "type": "string",
      "pattern": "^SCR-[0-9]{4}$"
    },
    "State": {
      "description": "The nine states, fixed by VDS S-5(3). A record may require a subset. It may not invent a tenth, and here it cannot.",
      "type": "string",
      "enum": [
        "default",
        "hover",
        "focus",
        "active",
        "selected",
        "disabled",
        "loading",
        "error",
        "success"
      ]
    },
    "StateContract": {
      "description": "Which of the nine states are required, which are drawn, which are built.\n\nStored as sorted, deduplicated vectors rather than sets so the serialised form is stable: a record's bytes must not depend on hash iteration order, or the register digest moves without the register moving.",
      "type": "object",
      "required": [
        "built",
        "drawn",
        "required"
      ],
      "properties": {
        "built": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/State"
          }
        },
        "drawn": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/State"
          }
        },
        "required": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/State"
          }
        }
      },
      "additionalProperties": false
    },
    "Status": {
      "description": "The lifecycle at VDS S-5(4). A directed path where skipping is forbidden.",
      "type": "string",
      "enum": [
        "proposed",
        "designed",
        "registered",
        "built",
        "verified",
        "deprecated",
        "retired"
      ]
    },
    "Timestamp": {
      "description": "UTC, second resolution, trailing Z. The one canonical form VDS writes.",
      "type": "string",
      "format": "date-time",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$"
    },
    "WarrantId": {
      "description": "A warrant record. The stage number is part of the identifier so the four series are independent.",
      "type": "string",
      "pattern": "^WARRANT-W[1-4]-[0-9]{3}$"
    }
  }
}
