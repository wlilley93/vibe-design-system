id: PROOF-20260725-173623
kind: ledger_staleness
status: failed
warrant_id: null
command: vds proof ledger_staleness
script: crates/vds-proof/src/ledger_staleness.rs
exit_code: 1
rows_considered: 2
rows_enforced: 2
rows_skipped_reasons: {}
violations:
- location: .vds/ledgers/figma.yaml
  rule: 'VDS S-4(2) ledger_staleness R2: every ledger has a staleness test, and a ledger without one decays with nothing to say so'
  expected: a staleness test in this build that regenerates .vds/ledgers/figma.yaml from its source and fails where the two disagree, as .vds/ledgers/screens.yaml has (VDS S-4(2))
  actual: .vds/ledgers/figma.yaml has no staleness test in this build, so nothing would notice it going out of date with whatever generated it. Either add its generator and its staleness test to this gate, or remove the file.
  severity: fatal
notes:
- the screens ledger's staleness test REGENERATES the ledger from its sources and compares the content, rather than comparing the source digest the ledger records. A source-digest comparison answers only whether the screens moved, so it certifies a hand-edited ledger clean, and a proof reading that ledger can then be flipped from failing to passing without touching a screen (VDS S-2(5)(4)).
- 'what this run does NOT reach: this build holds one staleness test, the screens ledger''s. For every other file in the ledgers directory it establishes only that no staleness test exists, never that the file is current, because there is no generator here to re-run against it. It reaches nothing outside the configured ledgers directory. And where the declared screens ledger is absent the run is a precondition failure at exit 2, so the other files in that directory go unexamined rather than passing.'
inputs_digest: sha256:bae82e28188c3c0e2f0866d36d4ab8f5988b770bdc209e83373ea24ca7c513fd
digest: sha256:59507179747daf0158f01166758fb866ba7ac42aabd028e2e88d6066f60ad744
designpack_digest: sha256:f9c369f3175c15169dafde47c01d7efc2f45c64090444f17f5b3f480e710ec5a
captured_at: 2026-07-25T17:36:16Z
capture_mode: automatic
invoked_by: manual
duration_ms: 0
