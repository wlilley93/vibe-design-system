# [2026] VJS-CC-VIBE-DESIGN-SYSTEM 1 - The subscriber's D2

**County Court, Halden J.** Submission `SUBMISSION-2026-07-31-182718`, convening
`CONVENING-county-2026-07-31-182723`. Full facts in SUBMISSION-VDS-010 (.vds/submissions/filed/).

D2 asks one question - is the thing that judges this surface itself reviewed, pinned and
unable to change silently - and the honest evidence differs by who answers. The kernel
holds gate source: five limbs, unchanged. A subscriber holds a released binary: it pins
the binary's digest and version and resolves the rest transitively, because a release is
a specific kernel commit and that commit carries the kernel's lock, where every named
failing-direction test resolves. The claim is smaller and must say so: doctor names the
seat, so MET in the two cases is never one number.

Option A (fold the designpack pin into D2) refused: pack integrity is `vds pack verify`
and D6's subject, and folding it in makes D2 two numbers wearing one name. Where the
provenance chain is absent, the disposition is NOT CHECKED with the missing link named -
not MET (nothing was witnessed) and not UNMET (UNMET tells a subscriber to fix what is
not theirs to fix, and a criterion that can never clear trains readers to ignore it).

The worked example must teach a move a subscriber can make: its lock pins the binary it
runs, never kernel source paths from a subscriber root. The interim's reasoning is
affirmed - an example teaching a kernel-only move is worse than one teaching nothing,
because it would be copied.

The failing direction survives every seat: a binary replaced under a subscriber must fail
by digest mismatch, and the test proving it lives in the kernel, pinned by the kernel's
lock - the same transitivity the subscriber relies on, exercised once for everyone.
