# [2026] VJS-CC-VIBE-DESIGN-SYSTEM 2 - The variant-registry counterpart

**County Court, Marbury J.** Submission `SUBMISSION-2026-07-31-182905`, convening
`CONVENING-county-2026-07-31-182909`. Full facts in SUBMISSION-VDS-011 (.vds/submissions/filed/).

The register's job is to hold the settled COUNT of components, and the Figma file is the
record of what is decided ([2026] VJS-CC-OPBOX 3 D1): `Nav` is one decision varying on
Style. Twenty-six records over thirteen sets would make the register disagree with the
decided artefact about the only fact a register exists to hold - option one refused. A
model that refuses the ordinary shape of real code (site-factory, SDS and Opbox all
realise sets as registries or composites) is not a model of code but a demand that code
model it, never enacted - option three refused.

Option two granted, with its condition mandatory: the counterpart is the module, and the
registry arm must prove the registry's keys equal the record's variant union exactly.
Bare option two proves "the module exists", weaker than the named check; with key-set
equality it proves more than the named check ever did. The union W1b wrote into the
`variant` prop is what makes this checkable - the contract was complete, and this order
gives the coordinate the meaning the contract was waiting for.

The arm activates on SHAPE - a closed variant union, a coordinate naming the module, and
a module the scanner structurally marks as a registry - never on the named arm's failure.
A fallback that fires on failure absorbs typos, the silent-absorption class condemned in
[2026] VJS-CC-VJS 12 the same day. Implementation confirmed the third condition is
load-bearing: without it, `Button.tsx` exporting `Button` (stem and exportName colliding
by ordinary convention) fell into the arm, caught by the existing suite.

The interim's red goes green by rule change alone. Measured after: a fresh generated
project reads parity PASS, 13 enforceable rows, 0 violations, with both failing
directions and the no-absorption control proven in the gate's own tests.
